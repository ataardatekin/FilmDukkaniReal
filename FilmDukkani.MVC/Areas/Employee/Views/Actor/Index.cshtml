@model List<Actor>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Employee/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <div class="row justify-content-between">
        <div class="row justify-content-between align-items-center">
            <h2 class="col-6">Aktör Listesi</h2>
            <a asp-action="Create" asp-controller="Actor" asp-area="Employee" class="btn btn-success col-2">
                <i class="fa-solid fa-plus"></i> Aktör Ekle
            </a>

            <table class="table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Aktör Adı</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var actor in Model)
                    {
                        <tr>
                            <td>@actor.Id</td>
                            <td>@actor.Name</td>
                            <td>

                                <a asp-action="Delete" asp-controller="Actor" asp-area="Employee" asp-route-id="@actor.Id" class="btn btn-danger">
                                    <i class="fa-solid fa-trash"></i>
                                </a>

                                <a asp-action="Update" asp-controller="Actor" asp-area="Employee" asp-route-id="@actor.Id" class="btn btn-warning">

                                    <i class="fa-solid fa-pen-to-square"></i>
                                </a>

                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>


